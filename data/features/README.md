# Extracted Features

Feature files will be saved here:

- `image_features.csv` - Extracted image features (embeddings, histograms, etc.)
- `audio_features.csv` - Extracted audio features (MFCCs, spectral features, etc.)
